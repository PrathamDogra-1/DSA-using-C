#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,pos=0;
    scanf("%d",&n);
    int list[n];
    for(int i=0;i<n;i++){
        scanf("%d ",&list[i]);
    }
    int value;
    scanf("%d",&value);
    for(int i=0;i<n;i++){
        while(value==list[i]){
            pos=i;
            for(int i=pos;i<n;i++){
                list[i]=list[i+1];
            }
            n--;
        }
        
    }
    for(int i=0;i<n;i++){
        printf("%d ",list[i]);
    }
    
    return 0;
}
